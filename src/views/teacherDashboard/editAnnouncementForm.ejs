<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Announcement</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h2 class="mb-4">✏️ Edit Announcement</h2>
        
        <form action="/teacher/announcement/update/<%= announcement._id %>" method="POST">
            <!-- Hidden Inputs for Class & Subject -->
            <input type="hidden" name="classNumber" value="<%= announcement.classNumber %>">
            <input type="hidden" name="subjectName" value="<%= announcement.subjectName %>">

            <div class="mb-3">
                <label class="form-label fw-bold">Title:</label>
                <input type="text" class="form-control" name="title" value="<%= announcement.title %>" required>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Message:</label>
                <textarea class="form-control" name="message" rows="4" required><%= announcement.message %></textarea>
            </div>

            <div class="d-flex gap-3">
                <button type="submit" class="btn btn-success">Update</button>
                <a href="/teacher/class/<%= announcement.classNumber %>/<%= announcement.subjectName %>" class="btn btn-secondary">🔙 Back</a>
            </div>
        </form>
    </div>
</body>
</html>
